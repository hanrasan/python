AI > 머신러닝 > 딥러닝

머신 러닝 학습 방법

지도 학습:
정답 제공
회귀 방식, 분류 방식

비지도 학습:
정답제공 X
연관 규칙 방식, 군집 방식

강화 학습:
보상을 주는 방식




데이터셋을 8:2로 훈련셋과 테스트셋으로 나눈다. 이를 데이터 분리라고 한다.
데이터 학습은 훈련셋으로 진행한다. 이후 학습모델이 생성된다.
학습모델의 결과를 예측하고 평가를 한다. 이를 실행하기 위해서 테스트셋으로 실험한다.